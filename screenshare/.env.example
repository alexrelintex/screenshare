# Copy to .env and edit. Both docker compose AND the Makefile read this file.
# Change these if something else already owns the default. Check with:
#   make ports
SIGNALING_PORT=8000
STATIC_PORT=5173
